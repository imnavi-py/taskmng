// import { CreateAgentDto } from '~/agents/agent/dto/create-agent.dto'

// export function createFormData(data: CreateAgentDto, fileBuffer: Buffer | null, fileName: string | null): FormData {
//   const formData = new FormData()
//   formData.append('params', JSON.stringify(data.params))
//   if (fileBuffer) {
//     formData.append('file', fileBuffer, fileName || 'uploaded_file')
//   }
//   return formData
// }
